const language = "javascript";

/*
let index = 0

while (index <= language.length) {
    let letter = language[index];
    if (letter === "a"){
        console.log(letter);
    }
    index++;
}
*/

for (let i = 0; i < language.length; i++) {
    let letter = language[i];
    if (letter === "a"){
        console.log(letter);
    }
}

//OK